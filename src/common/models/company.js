'use strict';

const fs = require("fs");

module.exports = function(Company) {
//     Company.afterRemote('prototype.patchAttributes', function(context, next) {
//     const req = context.req;

//     // if (req.body.oldCompanyLogo) {
//     //     fs.unlink('/storage/company/' + req.body.oldCompanyLogo, (err) => { console.log(err) });
//     // }
//     next();
//   });
};
